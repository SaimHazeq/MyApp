# Base URL for the FastAPI backend's v1 API. The Vite dev server also proxies
# /api to http://localhost:8000 (see vite.config.js), so this is mainly used
# for production builds where the frontend and backend are on different hosts.
VITE_API_BASE_URL=/api/v1
